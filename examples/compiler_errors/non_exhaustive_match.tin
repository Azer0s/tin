// compiler_errors/non_exhaustive_match.tin
// A match on an enum that does not cover all variants.

enum i32 Color =
  red:   0
  green: 1
  blue:  2

fn describe(c Color) string =
  match c:
    case Color.red:
      return "red"
    case Color.green:
      return "green"
