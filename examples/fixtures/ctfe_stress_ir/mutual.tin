fn{#pure} is_even(n i64) bool =
  if n == 0:
    return true
  return is_odd(n - 1)

fn{#pure} is_odd(n i64) bool =
  if n == 0:
    return false
  return is_even(n - 1)

fn main() i64 =
  if is_even(20):
    return 1
  return 0
