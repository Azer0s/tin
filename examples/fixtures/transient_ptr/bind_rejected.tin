// Binding a pointer-arithmetic result to a let is rejected outside
// `{#unsafe}`: the resulting view's lifetime cannot be tracked
// once it leaves the expression.

fn main() =
  let xs [u8] = [1 as u8, 2 as u8, 3 as u8]
  let p = &xs[0]
  let q = p + 1   // rejected
  echo q
