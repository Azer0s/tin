trait Animal =
  fn sound(this Animal) string = virtual

struct Dog(Animal) =
  name string
  fn Animal::sound(this Dog) string = return "woof"

fn pair() (Animal, i64) =
  let d = Dog{name: "rex"}
  return (d, 7)

fn main() =
  let p = pair()
  echo "{p.b} {p.a.sound()}"
