// `for ref` iteration variables alias storage in the source container;
// moving would steal the slot and leave a dangling alias.  Plain
// `for item in xs` is move-eligible -- it makes a per-iteration copy.
fn main() =
  let xs = ["a", "b", "c"]
  for ref item in xs:
    echo move item
