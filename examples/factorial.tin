// Iterative factorial
fn factorial(n i64) i64 =
  let result i64 = 1
  for let i i64 = 2; i <= n; i++:
    result = result * i
  return result

echo factorial(0)
echo factorial(1)
echo factorial(5)
echo factorial(10)
echo factorial(20)
